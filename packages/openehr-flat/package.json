{
  "name": "@ehrbase-ui/openehr-flat",
  "version": "0.0.0",
  "private": true,
  "description": "openEHR FLAT / simSDT (Marand) converter — form state <-> FLAT key/value map for EHRbase composition writes/reads (§7). Web-template-aware; FLAT path grammar included. Consumed by the M6 form engine.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./spec": "./src/spec.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@ehrbase-ui/openehr-web-template": "workspace:*"
  },
  "devDependencies": {
    "@ehrbase-ui/config-tsconfig": "workspace:*",
    "typescript": "6.0.3",
    "vitest": "4.1.7"
  }
}
