// openEHR spec coordinates this package targets. Kept in sync with spec.json
// by `pnpm regen` (ADR-0032 + 2026-05-30 addendum). Pinned to EHRbase 2.31.0
// (RM 1.1.0). A `const` literal infers the literal type — no annotation / no `as`.

export const SPEC_COMPONENT = "RM";
export const SPEC_VERSION = "1.1.0";
