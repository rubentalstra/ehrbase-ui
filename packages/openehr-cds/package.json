{
  "name": "@ehrbase-ui/openehr-cds",
  "version": "0.0.0",
  "private": true,
  "description": "openEHR CDS 2.0.1 — GDL2-aligned CdsRule authoring model (Zod): variable bindings + condition tree (when) + severity-graded actions (then). Per ADR-0021 a form-authored subset, not raw GDL2. Consumed by M9 CDS infrastructure.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./spec": "./src/spec.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@ehrbase-ui/config-tsconfig": "workspace:*",
    "typescript": "6.0.3",
    "vitest": "4.1.7"
  }
}
