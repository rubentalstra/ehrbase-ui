{
  "name": "@ehrbase-ui/observability",
  "version": "0.0.0",
  "private": true,
  "description": "Pino application logger + (M5) OTel SDK + Pino-OTLP transport + PHI redaction + health/ready checks. Framework-agnostic — apps register transports / request context at startup.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./log": "./src/log/index.ts"
  },
  "dependencies": {
    "pino": "10.3.1"
  },
  "devDependencies": {
    "@ehrbase-ui/config-tsconfig": "workspace:*",
    "@types/node": "25.9.1"
  }
}
