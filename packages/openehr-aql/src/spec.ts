// openEHR Query (AQL) coordinates this package targets. AQL is sent to EHRbase,
// so we track the AQL the server accepts (QUERY 1.1.0). A `const` literal infers
// the literal type.

export const SPEC_COMPONENT = "AQL";
export const SPEC_VERSION = "1.1.0";
