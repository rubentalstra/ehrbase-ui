{
  "name": "@ehrbase-ui/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Better Auth instance factory + service locator + RBAC guard + break-glass flow + Keycloak SSO bootstrap. Per ADR-0028 / ADR-0029. Framework-agnostic: the host app supplies the cookie-handling plugin (e.g. tanstackStartCookies) via the buildAuth() options object.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/auth-client.ts"
  },
  "dependencies": {
    "@better-auth/sso": "1.6.11",
    "@ehrbase-ui/audit": "workspace:*",
    "@ehrbase-ui/db-platform": "workspace:*",
    "@ehrbase-ui/http-bff": "workspace:*",
    "@ehrbase-ui/valkey": "workspace:*",
    "better-auth": "1.6.11",
    "drizzle-orm": "1.0.0-rc.4-273829f",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@ehrbase-ui/config-tsconfig": "workspace:*",
    "@types/node": "25.9.1"
  }
}
