{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "React (browser/SSR — extends base, adds DOM lib + react-jsx)",
  "extends": "./base.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  }
}
