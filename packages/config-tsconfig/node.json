{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Node (server-only packages — extends base, no DOM)",
  "extends": "./base.json",
  "compilerOptions": {
    "types": ["node"]
  }
}
