{
  "name": "@ehrbase-ui/demographic-adapter-fhir",
  "version": "0.0.0",
  "private": true,
  "description": "FHIR R4 Patient adapter for the DemographicProvider interface. Version-aware (R4/R4B only for v1.0; R5/R6 throw at construction — pure-additive per ADR-0033).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@ehrbase-ui/demographic-core": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@ehrbase-ui/config-tsconfig": "workspace:*",
    "@types/node": "25.9.1",
    "typescript": "6.0.3",
    "vitest": "4.1.7"
  }
}
