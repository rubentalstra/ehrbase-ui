// The web template is the EHRbase / Marand "Simplified Data Template" (web
// template) JSON shape — not a versioned openEHR spec component. We track the
// EHRbase 2.31.0 shape (ADR-0032). A `const` literal infers the literal type.

export const SPEC_COMPONENT = "WEB_TEMPLATE";
export const SPEC_VERSION = "ehrbase-2.31";
