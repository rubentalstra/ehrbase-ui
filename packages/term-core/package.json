{
  "name": "@ehrbase-ui/term-core",
  "version": "0.0.0",
  "private": true,
  "description": "TerminologyProvider interface (FHIR R4 Terminology Service shape) + shared FHIR client/adapter + the unconfigured `none` provider — per ADR-0034. Snowstorm-specific tweaks live in term-adapter-snowstorm.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./contract": "./src/contract.ts",
    "./fhir-double": "./src/fhir-double.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@ehrbase-ui/openehr-term": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@ehrbase-ui/config-tsconfig": "workspace:*",
    "@types/node": "25.9.1",
    "typescript": "6.0.3",
    "vitest": "4.1.7"
  }
}
