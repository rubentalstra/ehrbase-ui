# Development stack for ehrbase-ui.
# docs/architecture.md §18 — all images pinned to exact tags, never :latest.

services:
  ehrbase-db:
    # EHRbase ships a companion Postgres image that pre-creates the
    # ehrbase / ehrbase_restricted roles + the extensions EHRbase needs.
    # Vanilla postgres:18 does NOT have these, so EHRbase's Flyway migration
    # fails against it. The companion image is PG16-based (only 16.2 is
    # published) — this is the one place the stack is not on PG18. Keycloak's
    # DB below stays on vanilla postgres:18.4. See docs/architecture.md §18
    # note + REFERENCES drift list.
    image: ehrbase/ehrbase-v2-postgres:16.2
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      EHRBASE_USER_ADMIN: ehrbase
      EHRBASE_PASSWORD_ADMIN: ehrbase
      EHRBASE_USER: ehrbase_restricted
      EHRBASE_PASSWORD: ehrbase_restricted
    volumes:
      # PG16 image uses the classic /var/lib/postgresql/data mount.
      - ehrbase_pg:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 10s
      timeout: 5s
      retries: 5
    networks: [ehrbase-net]

  ehrbase:
    # Pin to an exact version. Never use :latest.
    image: ehrbase/ehrbase:2.31.0
    # EHRbase builds its OAuth2 JwtDecoder at startup by fetching Keycloak's
    # OIDC discovery doc, so it must not start until the realm EXISTS. The realm
    # is now applied by keycloak-config (not --import-realm), which runs AFTER
    # keycloak is healthy — so we gate on keycloak-config completing, not merely
    # on keycloak's health. Deterministic ordering, no boot-time error noise.
    # restart: unless-stopped is an orthogonal runtime-resilience policy
    # (recover from a later crash), not the startup-ordering mechanism.
    restart: unless-stopped
    depends_on:
      ehrbase-db: { condition: service_healthy }
      keycloak-config: { condition: service_completed_successfully }
    environment:
      DB_URL: jdbc:postgresql://ehrbase-db:5432/ehrbase
      # EHRbase 2.x two-user model: admin runs Flyway migrations, the
      # restricted user is the runtime connection. Both are created by the
      # companion postgres image above.
      DB_USER_ADMIN: ehrbase
      DB_PASS_ADMIN: ehrbase
      DB_USER: ehrbase_restricted
      DB_PASS: ehrbase_restricted
      SECURITY_AUTHTYPE: OAUTH
      # EHRbase 2.x wires its JwtDecoder from the STANDARD Spring Boot
      # resource-server property spring.security.oauth2.resourceserver.jwt.*.
      # Spring's env-var binding drops the hyphen in `issuer-uri`, so the
      # var is ...JWT_ISSUERURI (not ISSUER_URI). The arch-doc §18 names
      # (SECURITY_OAUTH_ISSUER_URI) bind to nothing → no JwtDecoder bean →
      # "APPLICATION FAILED TO START". See docs/architecture.md §18 note.
      #
      # ISSUERURI must MATCH the `iss` claim of the forwarded user token. The UI
      # obtains every Keycloak token via the BROWSER-facing URL `localhost:8180`
      # (the ui container resolves `localhost` to the host gateway — see the `ui`
      # service `extra_hosts` block — so even the server-side OIDC token/refresh
      # exchange hits localhost:8180). Keycloak therefore stamps `iss` =
      # http://localhost:8180/realms/ehrbase on EVERY token, NOT the internal
      # keycloak:8080 host. So EHRbase must validate that exact issuer string.
      # Because JWKSETURI is set explicitly, Spring builds the decoder from the JWK
      # set (reached INTERNALLY at keycloak:8080 — the host gateway isn't on this
      # network) and ONLY string-validates `iss` against ISSUERURI; it never
      # network-fetches ISSUERURI, so localhost:8180 being unreachable from this
      # container is fine. (A mismatch here = "The iss claim is not valid" → 401 on
      # every forwarded-token EHRbase call — composition CRUD, EHR provision, …)
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUERURI: http://localhost:8180/realms/ehrbase
      SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWKSETURI: http://keycloak:8080/realms/ehrbase/protocol/openid-connect/certs
      SECURITY_OAUTH2USERROLE: clinician
      SECURITY_OAUTH2ADMINROLE: admin
      ADMIN_API_ACTIVE: 'true'
    ports: ['8080:8080']
    networks: [ehrbase-net]

  # Shared platform Postgres instance. Hosts the Keycloak database plus the
  # app-owned `auth` + `demographic` databases. Each DB + its least-privilege
  # roles are created on first boot by the init scripts mounted below.
  platform-db:
    image: postgres:18.4-alpine
    environment:
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: keycloak
      POSTGRES_DB: keycloak
    volumes:
      # Postgres 18+ mount convention (see ehrbase-db above).
      - platform_pg:/var/lib/postgresql
      # First-boot init: create the app-owned databases + their least-privilege
      # roles. Runs once per fresh volume.
      - ./platform-db-init:/docker-entrypoint-initdb.d:ro
    # Exposed for host-side `pnpm dev` + `pnpm db:migrate`. Not exposed in a
    # production deployment (the app reaches it on the internal network).
    ports: ['5432:5432']
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U keycloak']
      interval: 10s
      timeout: 5s
      retries: 5
    networks: [ehrbase-net]

  keycloak:
    # CVE-2026-37981 floor (§5.12).
    image: quay.io/keycloak/keycloak:26.6.2
    # No `--import-realm`: realm + clients (+ dev users) are applied
    # declaratively, idempotently, and update-in-place by the keycloak-config
    # service below (adorsys/keycloak-config-cli). `--import-realm` could only
    # create-or-recreate a realm, never update it — ADR-0036.
    command: start-dev
    depends_on:
      platform-db: { condition: service_healthy }
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://platform-db:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: keycloak
      # Keycloak 26 renamed the bootstrap-admin vars; the old KEYCLOAK_ADMIN*
      # names are deprecated (KC-SERVICES0110).
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
      KC_HOSTNAME_STRICT: 'false'
      KC_HEALTH_ENABLED: 'true'
    ports: ['8180:8080']
    # Health gate on the management-port /health/ready endpoint (port 9000,
    # enabled by KC_HEALTH_ENABLED). Keycloak now reports UP as soon as the
    # server is ready — the realm is applied AFTER this by keycloak-config, so
    # realm-dependent services (ehrbase, ui) gate on keycloak-config's
    # completion, NOT on this healthcheck. The Keycloak 26 image has no
    # curl/wget, but it ships bash — so we use bash's /dev/tcp to speak raw HTTP.
    healthcheck:
      test:
        [
          'CMD',
          'bash',
          '-c',
          "exec 3<>/dev/tcp/localhost/9000; echo -e 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' >&3; grep -q '200 OK' <&3",
        ]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 30s
    networks: [ehrbase-net]

  # Keycloak configuration-as-code — one-shot, declarative, idempotent
  # (ADR-0036). Applies the realm + roles + both OIDC clients (ehrbase-ui,
  # ehrbase) from keycloak/config/ehrbase-realm.json and the four dev demo
  # users from keycloak/config/ehrbase-users.dev.json, UPDATING in place on
  # every up. This is the official replacement for the old bespoke kcadm shell
  # scripts (realm import + demo-user seed) — one tool, one source of truth.
  #
  # Secrets are injected via keycloak-config-cli variable substitution
  # ($(env:VAR) with IMPORT_VARSUBSTITUTION_ENABLED) so prod rotates via env,
  # no file edit. PRODUCTION excludes the dev users by pointing
  # IMPORT_FILES_LOCATIONS at ehrbase-realm.json only (this dev compose globs
  # *.json). keycloak-config-cli manages only the collections each file
  # defines, so ehrbase-users.dev.json (users only) never touches the baseline clients.
  #
  # Image: pinned to the 26.x-line build (no 26.6.x tag is published yet; the
  # Keycloak 26 admin API is stable across minors, so this is compatible with
  # our keycloak:26.6.2). Never :latest (Inviolable rule 5).
  keycloak-config:
    image: adorsys/keycloak-config-cli:6.5.1-26.5.5
    depends_on:
      keycloak: { condition: service_healthy }
    environment:
      KEYCLOAK_URL: http://keycloak:8080
      KEYCLOAK_USER: admin
      KEYCLOAK_PASSWORD: admin
      KEYCLOAK_AVAILABILITYCHECK_ENABLED: 'true'
      KEYCLOAK_AVAILABILITYCHECK_TIMEOUT: 120s
      IMPORT_VARSUBSTITUTION_ENABLED: 'true'
      IMPORT_FILES_LOCATIONS: '/config/*.json'
      KEYCLOAK_CLIENT_SECRET: ${KEYCLOAK_CLIENT_SECRET:-dev-only-rotate-in-prod}
    volumes:
      - ./keycloak/config:/config:ro
    networks: [ehrbase-net]
    restart: 'no'

  # Valkey: BSD-licensed Redis fork. ioredis works against it unchanged.
  # CVE floor 9.1.0 (§5.12).
  valkey:
    image: valkey/valkey:9.1.0-alpine
    command: ['valkey-server', '--appendonly', 'yes']
    volumes:
      - valkey_data:/data
    # Exposed for host-side `pnpm dev`. Not exposed in production.
    ports: ['6379:6379']
    networks: [ehrbase-net]
    healthcheck:
      test: ['CMD', 'valkey-cli', 'ping']
      interval: 10s
      timeout: 3s
      retries: 5

  # One-shot drizzle-kit migration runner. Uses the builder Docker stage so
  # it has pnpm + the source-tree drizzle migration files (the runner image
  # only ships the Nitro bundle). Runs the auth + demographic migrations before
  # the ui starts, idempotent — drizzle-kit's __drizzle_migrations bookkeeping
  # table means re-runs no-op.
  db-migrate:
    build:
      context: .
      dockerfile: Dockerfile
      target: migrator
    depends_on:
      platform-db: { condition: service_healthy }
    environment:
      AUTH_DB_OWNER_URL: postgres://auth_owner:auth_owner@platform-db:5432/auth
      DEMOGRAPHIC_DB_OWNER_URL: postgres://demographic_owner:demographic_owner@platform-db:5432/demographic
      AUDIT_DB_OWNER_URL: postgres://audit_owner:audit_owner@platform-db:5432/audit
    networks: [ehrbase-net]
    restart: 'no'

  ui:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      # EHRbase has no healthcheck (distroless image, no shell) and the UI
      # only calls it lazily at request time, so service_started is enough.
      # Keycloak + Valkey ARE gated on health since the BFF (Milestone 2)
      # talks to them on the auth path.
      ehrbase: { condition: service_started }
      # Better Auth registers its SSO provider via Keycloak OIDC discovery, so
      # the realm + ehrbase-ui client must exist first → gate on keycloak-config
      # completing, not merely on keycloak's health.
      keycloak-config: { condition: service_completed_successfully }
      valkey: { condition: service_healthy }
      platform-db: { condition: service_healthy }
      db-migrate: { condition: service_completed_successfully }
    # The Better Auth SSO plugin uses ONE issuer URL per provider — both the
    # server-side OIDC discovery fetch AND the browser redirect target use
    # the same URL. The browser needs `http://localhost:8180`; we therefore
    # make `localhost` inside the ui container resolve to the host gateway
    # so the SSR side can hit the same address (Docker Desktop ships this
    # as `host-gateway`).
    extra_hosts:
      - 'localhost:host-gateway'
    environment:
      # Dev compose runs in development mode. HSTS, secure-only cookies,
      # and the enforcing CSP all gate on isProduction; production
      # deployments override this in their orchestrator env.
      NODE_ENV: development
      PORT: '3000'
      EHRBASE_URL: http://ehrbase:8080/ehrbase/rest/openehr/v1
      # Keycloak is reached at localhost:8180 for EVERYTHING (redirect AND the
      # server-side discovery/token/JWKS exchange) — the `extra_hosts:
      # localhost:host-gateway` block below routes the container's `localhost`
      # to the host's published 8180 port. So every issued token carries a
      # STABLE `iss` of http://localhost:8180/realms/ehrbase, which is exactly
      # what EHRbase's JWT_ISSUERURI validates (docs/architecture.md §5.4; the
      # classic docker OIDC gotcha). KEYCLOAK_INTERNAL_ISSUER_URL is used only
      # for trustedOrigins + the M2 legacy path, never for token issuance.
      KEYCLOAK_ISSUER_URL: http://localhost:8180/realms/ehrbase
      KEYCLOAK_INTERNAL_ISSUER_URL: http://keycloak:8080/realms/ehrbase
      KEYCLOAK_CLIENT_ID: ehrbase-ui
      KEYCLOAK_CLIENT_SECRET: ${KEYCLOAK_CLIENT_SECRET:-dev-only-rotate-in-prod}
      KEYCLOAK_REDIRECT_URI: http://localhost:3000/api/auth/callback
      # Better Auth + SSO plugin (ADR-0028/0029). The SSO_KEYCLOAK_* vars
      # configure the OIDC provider Better Auth registers in the
      # `sso_provider` table on first request. KEYCLOAK_INTERNAL_ISSUER_URL
      # above mirrors the same issuer for the M2 legacy path during the
      # transition; both can point at the same realm.
      AUTH_DB_URL: postgres://auth_writer:auth_writer@platform-db:5432/auth
      AUTH_DB_OWNER_URL: postgres://auth_owner:auth_owner@platform-db:5432/auth
      # ≥32 char dev secret — Better Auth warns at startup below that floor.
      # Production injects a real value via the env (see .env.example).
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:-dev-only-rotate-in-prod-please-this-is-32-chars}
      BETTER_AUTH_URL: http://localhost:3000
      SSO_KEYCLOAK_PROVIDER_ID: keycloak
      # Single URL — see the `extra_hosts: localhost:host-gateway` block
      # above for why this is the externally-reachable URL even from
      # inside the ui container.
      SSO_KEYCLOAK_ISSUER: http://localhost:8180/realms/ehrbase
      SSO_KEYCLOAK_CLIENT_ID: ehrbase-ui
      SSO_KEYCLOAK_CLIENT_SECRET: ${KEYCLOAK_CLIENT_SECRET:-dev-only-rotate-in-prod}
      VALKEY_URL: redis://valkey:6379
      # Draft-at-rest encryption key (field-encryption.server.ts). Production
      # injects a real value via the env (see .env.example).
      DRAFT_ENCRYPTION_SECRET: ${DRAFT_ENCRYPTION_SECRET:-dev-only-rotate-in-prod}
      # Demographic identifier-pseudonymisation HMAC key (demographic-core, rule 12).
      AUDIT_PSEUDONYM_SECRET: ${AUDIT_PSEUDONYM_SECRET:-dev-only-rotate-in-prod}
      # Demographic provider (M7 — ADR-0031). Built-in Postgres adapter is the
      # only provider for now; full CRUD via the least-privilege
      # demographic_writer role. The external FHIR/HL7v2/PDQ adapter was removed
      # in the core refocus — the DemographicProvider interface is retained so a
      # wire adapter can be re-added behind a new ADR (no other value is valid).
      DEMOGRAPHIC_PROVIDER: ${DEMOGRAPHIC_PROVIDER:-builtin}
      DEMOGRAPHIC_DB_URL: postgres://demographic_writer:demographic_writer@platform-db:5432/demographic
      DEMOGRAPHIC_DB_OWNER_URL: postgres://demographic_owner:demographic_owner@platform-db:5432/demographic
      DEMOGRAPHIC_PARTY_NAMESPACE: ${DEMOGRAPHIC_PARTY_NAMESPACE:-demographic}
      # IHE ATNA access-audit trail (M7 foundation — ADR-0041). Append-only
      # `audit` DB via the least-privilege audit_writer role. ATNA_AUDIT_SOURCE_ID
      # / ATNA_ENTERPRISE_SITE_ID identify this node in emitted AuditMessages; the
      # syslog forwarder to an external Audit Record Repository is M9.
      AUDIT_DB_URL: postgres://audit_writer:audit_writer@platform-db:5432/audit
      ATNA_AUDIT_SOURCE_ID: ${ATNA_AUDIT_SOURCE_ID:-ehrbase-ui-dev}
      # Dev demo data (docs/DEV-DEMO-DATA.md; CLAUDE.md rule 14). When 'true' the
      # first /admin/patients load seeds demo demographic patients so the UI + e2e
      # are observable. The flag is the SINGLE gate (read at runtime; the prod
      # build inlines NODE_ENV so it can't guard) — production must not set it true.
      SEED_DEMO_DATA: ${SEED_DEMO_DATA:-true}
      SESSION_IDLE_TIMEOUT_SECONDS: '900'
      SESSION_ABSOLUTE_TIMEOUT_SECONDS: '43200'
      LOG_LEVEL: info
      APP_VERSION: dev
    ports: ['3000:3000']
    networks: [ehrbase-net]

  # Terminology server — the dev backend for the external terminology adapters
  # (TERMINOLOGY_PROVIDER=generic-fhir | snowstorm; ADR-0034, ADR-0022). Behind
  # the `terminology` profile so the default `docker compose up` stays
  # unconfigured (TERMINOLOGY_PROVIDER=none); start it with
  # `docker compose --profile terminology up`. Tag-pinned (no :latest — rule 5).
  #
  # This HAPI image exposes the FHIR R4 Terminology Service ($expand / $lookup /
  # $validate-code) — point the GENERIC-FHIR adapter at it:
  #   TERMINOLOGY_PROVIDER=generic-fhir
  #   TERMINOLOGY_FHIR_BASE=http://localhost:8094/fhir
  # The SNOWSTORM adapter (the v1.0 default) is provided by the `snowstorm-es` +
  # `snowstorm` services below, behind their own heavy opt-in `snomed` profile.
  # Unit + contract tests use the in-memory FHIR terminology double — no Docker.
  terminology-fhir:
    image: hapiproject/hapi:v8.8.0-1
    profiles: ['terminology']
    environment:
      hapi.fhir.fhir_version: R4
      hapi.fhir.default_encoding: json
      spring.datasource.url: jdbc:h2:mem:hapitx;DB_CLOSE_DELAY=-1
    ports: ['8094:8080']
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'wget -q -O /dev/null http://localhost:8080/fhir/metadata || exit 1',
        ]
      interval: 15s
      timeout: 10s
      retries: 20
      start_period: 90s
    networks: [ehrbase-net]

  # ── SNOMED CT terminology server (TERMINOLOGY_PROVIDER=snowstorm, the default) ─
  # Heavy: Elasticsearch + Snowstorm need ≈ 8 GB RAM, so they sit behind their own
  # opt-in `snomed` profile → `docker compose --profile snomed up`. Then set
  #   TERMINOLOGY_PROVIDER=snowstorm
  #   TERMINOLOGY_FHIR_BASE=http://localhost:8095/fhir
  # IMPORTANT: Snowstorm starts EMPTY — a SNOMED CT RF2 edition must be imported via
  # its native API (license-gated; https://www.snomed.org or your national release
  # centre). ZERO-SETUP alternative (no local stack, no profile): point
  #   TERMINOLOGY_FHIR_BASE=https://snowstorm.ihtsdotools.org/fhir
  # at the public SNOMED International FHIR sandbox (international edition, read-only).
  # Tag-pinned (no :latest — rule 5). Mirrors github.com/IHTSDO/snowstorm compose.
  snowstorm-es:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.1
    profiles: ['snomed']
    environment:
      discovery.type: single-node
      xpack.security.enabled: 'false'
      ES_JAVA_OPTS: -Xms4g -Xmx4g
    ulimits:
      memlock: { soft: -1, hard: -1 }
    mem_reservation: 4g
    volumes:
      - snowstorm_es:/usr/share/elasticsearch/data
    healthcheck:
      test: ['CMD-SHELL', 'curl -fs http://localhost:9200 >/dev/null || exit 1']
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 60s
    networks: [ehrbase-net]

  snowstorm:
    image: snomedinternational/snowstorm:10.11.2
    profiles: ['snomed']
    depends_on:
      snowstorm-es:
        condition: service_healthy
    # Verbatim from the official IHTSDO/snowstorm compose, with the ES url pointed
    # at our `snowstorm-es` service.
    entrypoint: java -Xms2g -Xmx4g --add-opens java.base/java.lang=ALL-UNNAMED --add-opens=java.base/java.util=ALL-UNNAMED -cp @/app/jib-classpath-file org.snomed.snowstorm.SnowstormApplication --elasticsearch.urls=http://snowstorm-es:9200
    ports: ['8095:8080']
    mem_reservation: 4g
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'wget -q -O /dev/null http://localhost:8080/fhir/metadata || exit 1',
        ]
      interval: 20s
      timeout: 10s
      retries: 40
      start_period: 180s
    networks: [ehrbase-net]

volumes:
  ehrbase_pg:
  platform_pg:
  valkey_data:
  snowstorm_es:

networks:
  ehrbase-net:
    driver: bridge
